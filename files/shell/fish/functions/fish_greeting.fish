function fish_greeting -d "Greeting"
    #_logo

    #echo ""
    #command neofetch

    #set_color $fish_color_autosuggestion
    #uname -nmsr
    #command uptime
    #set_color normal

    # Change to the directory if a WIP directory exists
    # set -l HAS_WIP (l | command grep wip | wc -l)
    # if [ ! $HAS_WIP -eq 0 ]
    #     set -l cwd (p wip | sed "s:^$HOME:~:") # Replace the user's home directory with ~

    #     echo ""
    #     e_warning "A work in progress directory found at $cwd"
    #     g wip
    # end

    # set_color normal
end
